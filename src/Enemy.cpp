#include "Enemy.h"

Enemy::Enemy(int x, int y, sf::Texture const& texture, int spriteX, int spriteY, int spriteW, int spriteH, float gravity) :
AliveEntity::AliveEntity(x, y, texture, spriteX, spriteY, spriteW, spriteH, gravity){

}
